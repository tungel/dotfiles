# Manual step to configure ranger

Run the `configure-ranger.sh` script.

# Reference commands

Reload `~/.config/ranger/rc.conf`

    :eval fm.source(fm.confpath('rc.conf'))

Reload `~/.config/ranger/rifle.conf`

    :eval fm.rifle.reload_config()

